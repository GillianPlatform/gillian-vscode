@config manual_fold : true

// === Folding and Unfolding ===
//
// Add fold and/or unfold tactics to make this function verify.
//
// If you are stuck, you can find a hint on the hint folder.

predicate list(+x, alpha) {
    (x == null) * (alpha == nil);
    (x -> #v, #z) * list(#z, #beta) * (alpha == #v::#beta)
}

{ (v == #v) }
function list_allocate_node(v){
  t := new(2);
  [t] := v;
  return t
}
{ list(ret, [ #v ]) }
